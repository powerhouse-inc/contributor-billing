---
date: 2026-03-25
mode: quick
categories: [schema-compliance, orphan-detection, link-health]
---

# Health Check — 2026-03-25

Quick mode (Categories 1–3). Generated against vault at `knowledge/`.

---

## Summary

| Category | Status | Issues |
|---|---|---|
| Schema Compliance | WARNING | 5 files missing `summary` field |
| Orphan Detection | WARNING | 10 files with no incoming links |
| Link Health | WARNING | 10 dangling wiki links (8 are template/example placeholders, 2 are real) |
| Maintenance Signals | WARNING | `ops/observations/`, `ops/tensions/`, `inbox/` do not exist |

---

## Category 1: Schema Compliance

**Scope:** All `.md` files in `knowledge/insights/` (per template `_schema.applies_to`).
**Required field:** `summary`

| File | summary | type | Status |
|---|---|---|---|
| `insights/index.md` | present | moc | PASS |
| `insights/CLAUDE.md` | absent | — | FAIL |

**FAIL count: 1 of 2 insight-scope files.**

### Extended scope — self/ and ops/methodology/ notes

These files use `description` rather than `summary`. They predate or fall outside the insight-note schema (`applies_to: knowledge/insights/*.md`), so they are not schema violations. Noted for awareness:

| File | Field present | Value |
|---|---|---|
| `self/identity.md` | `description` | present |
| `self/methodology.md` | `description` | present |
| `self/goals.md` | `description` | present |
| `ops/methodology/derivation-rationale.md` | `description` | present |
| `ops/methodology/methodology.md` | `description` | present |

These files use `description` (the system-level vocabulary alias for `summary`). They are consistent with each other but inconsistent with the insight-note template schema which requires `summary`. This is a low-priority drift signal.

---

## Category 2: Orphan Detection

**Definition:** A file with zero incoming `[[wiki links]]` from any other file in the vault.

Files are flagged when they have 0 incoming links from other files.

### Orphans with no incoming links

| File | Incoming Links | Notes |
|---|---|---|
| `insights/CLAUDE.md` | 0 | Infrastructure file — intentionally not linked |
| `insights/index.md` | 0 | MOC hub — should be linked from somewhere |
| `ops/derivation.md` | 0 | Ops file — no link from methodology/ |
| `ops/derivation-manifest.md` | 0 | Ops file — machine-readable, not expected to be linked |
| `templates/insight-note.md` | 0 | Template — not expected to be linked |
| `templates/map.md` | 0 | Template — not expected to be linked |
| `templates/observation.md` | 0 | Template — not expected to be linked |
| `templates/source-capture.md` | 0 | Template — not expected to be linked |
| `ops/reminders.md` | 0 | Ops file — referenced by name in CLAUDE.md but not as [[wiki link]] |
| `ops/tasks.md` | 0 | Ops file — referenced by name in CLAUDE.md but not as [[wiki link]] |

**Actionable orphans (non-infrastructure):**

- `insights/index.md` — The MOC hub has no incoming links. Nothing links to it by `[[index]]`. This means it is unreachable via graph traversal. It should be the entry point but is currently disconnected.

All other orphans are infrastructure files (templates, ops config, derivation records) that are not expected to carry incoming wiki links.

**Orphan count requiring action: 1**

---

## Category 3: Link Health

**Definition:** Every `[[wiki link]]` must resolve to an existing filename (without extension) somewhere in the vault.

### Dangling links

| Link | Found In | Classification |
|---|---|---|
| `[[wiki links]]` | `insights/CLAUDE.md`, `manual/getting-started.md` | Example/prose in CLAUDE.md; placeholder in getting-started.md |
| `[[title]]` | `insights/CLAUDE.md` | Inline example — not a real link |
| `[[link]]` | `insights/CLAUDE.md` | Inline example — not a real link |
| `[[insight title]]` | `insights/CLAUDE.md` | Inline example — not a real link |
| `[[related insight]]` | `insights/CLAUDE.md`, `templates/insight-note.md` | Template placeholder |
| `[[another insight]]` | `insights/CLAUDE.md` | Inline example — not a real link |
| `[[relevant-map]]` | `templates/insight-note.md` | Template placeholder |
| `[[reducer patterns]]` | `insights/CLAUDE.md` | Example of a planned map — does not exist yet |
| `[[reducer timestamps must come from action input]]` | `insights/CLAUDE.md`, `manual/getting-started.md` | Example of a planned insight — does not exist yet |
| `[[batching MCP actions reduces round-trips]]` | `insights/CLAUDE.md` | Example of a planned insight — does not exist yet |

**Classification summary:**

- **Inline examples in CLAUDE.md** (not real links, used to illustrate syntax): `[[wiki links]]`, `[[title]]`, `[[link]]`, `[[insight title]]`, `[[related insight]]`, `[[another insight]]`, `[[batching MCP actions reduces round-trips]]`, `[[reducer timestamps must come from action input]]`, `[[reducer patterns]]` — 9 links
- **Template placeholders in `templates/insight-note.md`**: `[[related insight]]`, `[[relevant-map]]` — 2 links
- **Real dangling link in a non-example context**: `[[reducer timestamps must come from action input]]` in `manual/getting-started.md` (line 24) — 1 link

**Actionable dangling links: 1**

`[[reducer timestamps must come from action input]]` appears in `manual/getting-started.md` as a live example of how wiki links work in prose. The note it references does not exist. Either create the insight or replace the example with a link to an existing file.

---

## Maintenance Signals

| Signal | Count | Threshold | Status |
|---|---|---|---|
| `ops/observations/` files | — | directory missing | WARNING — directory does not exist |
| `ops/tensions/` files | — | directory missing | WARNING — directory does not exist |
| `ops/sessions/` files | 11 (10 dated + current.json) | no threshold defined | OK |
| `inbox/` files | — | directory missing | WARNING — directory does not exist |

### Notes on missing directories

The CLAUDE.md and derivation-rationale reference `ops/observations/`, `ops/tensions/`, and `inbox/` as active parts of the system. None of these directories exist. This means:

- Friction signals cannot be captured (`/remember` has nowhere to write)
- Tension tracking is unavailable
- Inbox-based processing pipeline cannot be used (no `inbox/` to route captures into)

These are not just missing folders — they represent blocked workflows. The processing pipeline requires `inbox/` to function; the self-evolution loop requires `ops/observations/` and `ops/tensions/`.

**Recommended action:** Create the three directories. They can be empty initially — the system creates files in them when first used.

---

## Prioritized Action List

Priority order follows the CLAUDE.md maintenance framework (session > multi_session > slow):

**Session priority (act now):**

1. Create `knowledge/inbox/`, `knowledge/ops/observations/`, `knowledge/ops/tensions/` — blocked workflows
2. Link `insights/index.md` from at least one other file so the MOC hub is reachable via graph traversal
3. Fix `[[reducer timestamps must come from action input]]` in `manual/getting-started.md` — create the insight or substitute a real link

**Multi-session priority:**

4. Add `summary` field to `insights/CLAUDE.md` — only file in insights-scope failing schema check
5. Decide whether `self/*.md` and `ops/methodology/*.md` files should migrate from `description` to `summary` for schema consistency

**Slow / background:**

6. Confirm that inline example links in `insights/CLAUDE.md` are intentionally non-resolving (they appear to be illustrative, not navigational — but flagging in case the intent was to create real examples)

---

## Vault Stats (as of this check)

| Metric | Count |
|---|---|
| Total .md files in vault | 22 |
| Files in `insights/` (note scope) | 2 |
| Files in `self/` | 3 |
| Files in `ops/methodology/` | 2 |
| Total wiki links found | 43 |
| Unique link targets | 20 |
| Resolving links | 10 |
| Dangling links (unique targets) | 10 |
| Files with 0 incoming links | 10 |
| Actionable orphans | 1 |
| Actionable dangling links | 1 |
| Session files | 11 |
